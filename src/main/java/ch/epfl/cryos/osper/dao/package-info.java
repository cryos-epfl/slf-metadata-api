/**
 * Contains the data access objects.
 * Interfaces in this package should have a 'Dao' name suffix (e.g. ExplosionDao)
 * Hibernate Dao implementations in this package should have a 'HbnDao' name suffix
 * (e.g. ExplosionHbnDao) and are normally annotated with {@code @Repository}.
 */
package ch.epfl.cryos.osper.dao;